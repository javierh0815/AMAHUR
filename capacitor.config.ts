import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.amahur.app',
  appName: 'registrapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
